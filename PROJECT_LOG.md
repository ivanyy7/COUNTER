## PROJECT_LOG для COUNTER

### 1. Краткое описание
- Рабочий лог проекта `COUNTER`: фиксируем ключевые шаги, решения и изменения относительно плана.

### 2. Ключевые шаги и история
- День 1, 00:00 — создан базовый каркас файлов `PROJECT_MAP.md` и `PROJECT_LOG.md`. Детальный план (`PLAN.md`) будет добавлен позже по ТЗ.
- День 1, 16:23 — инициализирован Expo-проект в подпапке `mobile`, подключён телефон через Expo Go и реализован первый экран-счётчик (значение 0, кнопки «+» и «Сброс»).

### 3. Заметки и решения
- Решено: Expo-проект для мобильного приложения создаём в подпапке `mobile` внутри репозитория `COUNTER`. Все команды для Expo (например, `npx expo start`) выполняем в терминале, где видно `PS C:\Work\COUNTER\mobile>`. Это правило нужно, чтобы не путать корень репозитория и директорию мобильного приложения.

### 4. Возможные следующие шаги
- Составить `PLAN.md` по ТЗ.
- Определить структуру директорий и обновить `PROJECT_MAP.md` (учесть подпапку `mobile` и файлы мобильного приложения).
